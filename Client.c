#include "Client.h"

void client_handle(struct work_struct *work){
    // struct client_work *cw = container_of(work,struct client_work , work);
    // struct socket *sock = cw->client_sock;

    // we need to handle the client_work !! 
  
}


EXPORT_SYMBOL(client_handle);
MODULE_LICENSE("GPL");